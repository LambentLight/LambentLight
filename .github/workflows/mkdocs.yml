name: mkdocs

on:
  push:
    paths:
      - "**.md"
      - "**.png"
      - "mkdocs.yml"
      - ".github/workflows/mkdocs.yml"

jobs:
  mkdocs:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@master
      - uses: justalemon/MarkdownMaterial@master
        with:
          cname: "lambentlight.ml"
      - uses: justalemon/DeployToBranch@master
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
